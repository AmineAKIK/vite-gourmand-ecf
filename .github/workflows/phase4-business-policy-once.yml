name: Phase 4 business policy one-shot

on:
  push:
    branches:
      - v1/business-policy

permissions:
  contents: write

jobs:
  refactor:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v1/business-policy
          fetch-depth: 0
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo_mysql, zip
          coverage: none
          tools: composer:v2, phpunit:10, phpstan:2.2, php-cs-fixer:3
      - run: python3 -m py_compile bin/phase4_business_policy_refactor.py
      - run: python3 bin/phase4_business_policy_refactor.py
      - run: composer install --prefer-dist --no-progress --no-interaction
      - run: find src public tests -type f -name '*.php' -print0 | xargs -0 -n1 php -l >/dev/null
      - run: phpunit --configuration phpunit.xml
      - run: phpstan analyse --configuration=phpstan.neon --no-progress
      - name: Commit refactor
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src
          git commit -m 'Phase 4: centralize commercial business policy'
          git push origin HEAD:v1/business-policy
