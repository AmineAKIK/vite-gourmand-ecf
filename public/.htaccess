Options -Indexes
RewriteEngine On
RewriteBase /

# Les documents de facturation sont privés. Même si une ancienne copie existe encore
# physiquement sous public/uploads/facturation, elle doit passer par l'application et finir en 404.
RewriteRule ^uploads/facturation(?:/.*)?$ index.php [L]

# Bloquer l'accès aux fichiers sensibles
<FilesMatch "\.(env|sql|json|lock|md)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Tout rediriger vers index.php sauf fichiers statiques
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]

# Headers de sécurité
Header set X-Content-Type-Options "nosniff"
Header set X-Frame-Options "SAMEORIGIN"
Header set X-XSS-Protection "1; mode=block"
Header set Referrer-Policy "strict-origin-when-cross-origin"

<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
</IfModule>
